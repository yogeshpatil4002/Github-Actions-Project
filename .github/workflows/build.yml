name: 🔨 Build, Analyze, and Push to ECR

on:
  push:
    branches:
      - main
      - dev
      - stg
      - prod

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REPO_NAME: ${{ secrets.ECR_REPO_NAME }}

jobs:
  build:
    name: 🧪 Build & Analyze
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🛠 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Install Dependencies
        run: npm install
        working-directory: ./app

      - name: ✅ Lint the Code
        run: npm run lint || echo "Lint warnings ignored"
        working-directory: ./app

      - name: 🧪 Run Unit Tests
        run: npm test
        working-directory: ./app

      - name: 🔍 SonarCloud Code Analysis
        uses: SonarSource/sonarcloud-github-action@v2
        with:
          projectBaseDir: ./app
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
          SONAR_PROJECT_KEY: ${{ secrets.SONAR_PROJECT_KEY }}
          SONAR_ORGANIZATION: ${{ secrets.SONAR_ORGANIZATION }}

      - name: 🔢 Semantic Versioning
        run: npx semantic-release
        working-directory: ./app
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: ⚙️ Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🔐 Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 🐳 Build Docker Image
        run: docker build -t $ECR_REPO_NAME ./app

      - name: 🏷️ Tag Docker Image
        run: |
          IMAGE_TAG=${{ github.ref_name }}-${{ github.sha }}
          IMAGE_URI="${{ steps.login-ecr.outputs.registry }}/$ECR_REPO_NAME:$IMAGE_TAG"
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV
          docker tag $ECR_REPO_NAME $IMAGE_URI

      - name: 🛡️ Run Trivy Security Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_URI }}
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'

      - name: 📤 Push Docker Image to ECR
        run: docker push ${{ env.IMAGE_URI }}

      - name: 📧 Send Email Notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "✅ CI Build Completed for ${{ github.ref_name }}"
          to: ${{ secrets.EMAIL_TO }}
          from: ${{ secrets.EMAIL_USER }}
          body: |
            GitHub Actions build completed successfully.
            Image pushed: ${{ env.IMAGE_URI }}

      - name: 📢 Slack Notification
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"✅ *CI Build Success:* Branch `${{ github.ref_name }}` pushed to ECR.\nImage: `${{ env.IMAGE_URI }}`"}' \
          ${{ secrets.SLACK_WEBHOOK_URL }}
